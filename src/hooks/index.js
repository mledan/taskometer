/**
 * Custom Hooks Index
 *
 * Export all custom hooks from a single entry point.
 */

export { usePWA, default as usePWADefault } from './usePWA';
export { useKeyboardShortcuts, DEFAULT_SHORTCUTS } from './useKeyboardShortcuts';
