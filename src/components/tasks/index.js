/**
 * Tasks Components Index
 *
 * Export all task-related components from a single entry point.
 */

export { default as TaskInput } from './TaskInput';

// Re-export legacy component names for backwards compatibility
export { default as AddItemForm } from './TaskInput';
